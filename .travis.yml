language: cpp
dist: trusty
sudo: false
os: linux

matrix:
  include:
    - env: COMPILER=g++-8 BUILD=Debug STANDARD=14
      compiler: gcc
      addons:
        apt:
          update: true
          sources:
            - sourceline: 'ppa:mhier/libboost-latest'
            - ubuntu-toolchain-r-test
          packages:
            - g++-8
            - boost1.67
    - env: COMPILER=g++-8 BUILD=Release STANDARD=14
      compiler: gcc
      addons:
        apt:
          update: true
          sources:
            - sourceline: 'ppa:mhier/libboost-latest'
            - ubuntu-toolchain-r-test
          packages:
            - g++-8
            - boost1.67

before_script:
  - wget https://cmake.org/files/v3.9/cmake-3.9.1-Linux-x86_64.sh
  - sudo sh cmake-3.9.1-Linux-x86_64.sh -- --skip-license --prefix=/usr
 # - apt-get install libboost-dev -y
script:
  - cmake .
  - cmake --build .
#  - cmake --build . --target test
  - cmake --build . --target package
deploy:
  provider: script
  skip_cleanup: true
  script:
    - curl -T hcch-0.0.$TRAVIS_BUILD_NUMBER-Linux.deb -uangelskieglazki:$BINTRAY_API_KEY "https://api.bintray.com/content/angelskieglazki/aeco/hcch/$TRAVIS_BUILD_NUMBER/hcch-0.0.$TRAVIS_BUILD_NUMBER-Linux.deb;deb_distribution=trusty;deb_component=main;deb_architecture=amd64;publish=1"